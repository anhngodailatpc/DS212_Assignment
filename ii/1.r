MAX_NEWCASES_BRA <- max(BRA$new_cases, na.rm = T)
MAX_NEWCASES_CHL <- max(CHL$new_cases, na.rm = T)
MAX_NEWCASES_VEN <- max(VEN$new_cases, na.rm = T)

MIN_NEWCASES_BRA <- min(BRA$new_cases, na.rm = T)
MIN_NEWCASES_CHL <- min(CHL$new_cases, na.rm = T)
MIN_NEWCASES_VEN <- min(VEN$new_cases, na.rm = T)

MAX_NEWDEATHS_BRA <- max(BRA$new_deaths, na.rm = T)
MAX_NEWDEATHS_CHL <- max(CHL$new_deaths, na.rm = T)
MAX_NEWDEATHS_VEN <- max(VEN$new_deaths, na.rm = T)

MIN_NEWDEATHS_BRA <- min(BRA$new_deaths, na.rm = T)
MIN_NEWDEATHS_CHL <- min(CHL$new_deaths, na.rm = T)
MIN_NEWDEATHS_VEN <- min(VEN$new_deaths, na.rm = T)