AVG_NEWCASES_BRA <- mean(BRA$new_cases, na.rm = T)
AVG_NEWCASES_CHL <- mean(CHL$new_cases, na.rm = T)
AVG_NEWCASES_VEN <- mean(VEN$new_cases, na.rm = T)

AVG_NEWDEATHS_BRA <- mean(BRA$new_deaths, na.rm = T)
AVG_NEWDEATHS_CHL <- mean(CHL$new_deaths, na.rm = T)
AVG_NEWDEATHS_VEN <- mean(VEN$new_deaths, na.rm = T)